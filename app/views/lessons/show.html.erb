<div class="container">
  <h1><%= @lesson.notion.name %></h1>
  <p> <%= @lesson.notion.description %></p>
  <p><%= @lesson.teacher.full_name %></p>
  <%= link_to "Edit", edit_lesson_path(@lesson) %>

</div>
